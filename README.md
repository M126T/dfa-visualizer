# dfa-visualizer
An interactive web tool that visualizes the step-by-step minimization of Deterministic Finite Automata (DFA). Features custom inputs, equivalence partitioning tables, and dynamic graph rendering.
